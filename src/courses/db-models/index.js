const { CourseModel } = require('../db-models/Course')
const { SectionModel } = require('../db-models/Section')
const { ExerciseModel } = require('../db-models/Exercise')
const { CategoryModel } = require('../db-models/Category')

module.exports = {
    CourseModel,
    SectionModel,
    ExerciseModel,
    CategoryModel
}